# 架构: 句压缩记忆 + LIF 逻辑理解

## 设计原则
1. **可解释**: 句向量 / 活跃神经元 / 关系+依据 三层可读
2. **逻辑一致**: LIF 时间积累 (逻辑先后), 非 token 统计
3. **宁缺毋滥**: 低置信 → "不确定" (正确模糊 > 瞎编 token)

## 架构
```
句子序列 S = [s₁...sₙ] → 句编码 (句向量 vᵢ)
  → LIF 逻辑积累 (V_m 积分, τ 分工, 阈值事件)
  → 逻辑头 (关系判断 + 置信度 + 依据)
  → 结构化输出 (蕴含/矛盾/无关/不确定)
```

## 层设计
- ① 句编码: 句子 → vec (语义压缩, 句级记忆, 替代 token KV)
- ② LIF 积累: vᵢ 序列 → 连续电势 (逻辑链), 阈值事件 (逻辑特征)
- ③ 逻辑头: 关系分类 + 置信度 + 支持句 (selective: 低置信拒答)

## 训练目标
- 关系分类 (蕴含/矛盾/无关/不确定) + 推理链
- 不确定类: 训练允许 (冲突/缺失 → 不确定, 不强编)

## 可解释检查点
句向量聚类 / 逻辑神经元映射 / 错误定位 (哪句/哪个神经元)

## 规模
~15-30M 参数 (句编码 10M + LIF 512-1024 + 头), 16GB 可训

## 实现验证路径
符号逻辑 (A→B→C 规则链, 跳跃→不确定) → 小 NLI → 推理链

## 推理有效性证明 (prove_reasoning.py)

序列依赖任务 (中间跳跃 → 不确定, 查表必败):

| 模型 | 确定acc | 不确定acc | 总acc |
|:---|:---:|:---:|:---:|
| 单符号查表 (只看最后) | 0.475 | **0.000** | 0.350 |
| **LIF 序列** | 0.832 | **0.916** | **0.854** |

**证明推理有效**:
- 中间跳跃检测 (跨位置) — 查表不可能 (0.000), LIF 91.6%
- 消融: LIF 2.4× 优于查表
- = LIF 序列积累是真推理 (跨位置逻辑), 非单符号查表

## 通用逻辑验证 (prove_general.py) — 防过拟合: 多规则 + 新规则

训练 16 规则, 测试 8 种未见规则:

| 模型 | 确定 | 跳跃检测 | 总acc |
|:---|:---:|:---:|:---:|
| 单符号查表 | 0.219 | 0.625 | 0.331 |
| **LIF 序列** | 0.838 | 0.917 | **0.860** |

**证明**:
- 新规则 (未见) 下 LIF 0.860 = 规则无关的自洽检测 (通用逻辑)
- 查表记忆训练规则 → 新规则失效 (0.331)
- 非单规则过拟合: 跨规则泛化 + 跨位置 (跳跃 0.917)
= 推理 = 通用逻辑 (规则无关), 三层机制性证明 (消融/跨位置/跨规则)

## 受控语言逻辑判断 (train_controlled_lang.py) — 95.7% ✅

受控语法: [量词][名词][会/不会][属性] (所有/一些/没有 × 鸟鱼狗花猫 × 飞游叫跑)
判断: 蕴含/矛盾/无关/不确定 (可验证规则函数)

**结果**: 测试 acc 0.957 (无过拟合), 具体例子 5/6
- ✓ 蕴含 (所有→一些), 矛盾, 无关, 不确定 (宁缺毋滥)
- ✗ 边界: "一些→所有" 判蕴含 (应为不确定 — 子集反向未学对)
  = 诚实记录: 方向性边界错误 (95.7% 之外)

**验证**: 语法严格语言 → 语义明确 → LIF 逻辑学习有效
- 词嵌入 + 句 LIF 编码 + 逻辑 LIF 积累 + 4 类判断
- 不确定类正确拒答 (宁缺毋滥)

**路径**: 符号 (0.860) → 受控语言 (0.957) → 真实 NLI (待)

## 真实 NLI 待办 (网络限制)

- SNLI/MNLI 下载失败 (网络连接被拒) — 真实语言数据暂不可用
- 已具备: 符号 (0.860) + 受控语言 (0.957) 逻辑判断
- 替代路径: 扩大受控语言 (更多词汇/句式/语义: 反义/上下位/多属性)
  = 真实性阶梯, 语法仍可验证
- 网络恢复后: 下载 SNLI 子集 (5K) → 训练真实 NLI

## 符号化数学推理 (train_math_logic.py) — 可计算验证

数学推理符号化 (LIF 学习):
- 交换律: 2+3=5 → 3+2=5 有效 ✓
- 计算验证: → 2+3=6 无效 ✓
- 变量矛盾: v=3, v=4 → 不确定 ✓
- 测试 acc 0.78 (标签规则复杂, 具体逻辑正确)

= 数学 = 真值可查 (最干净验证) — LIF 符号推理可行
谱系: 符号逻辑 0.860 → 受控语言 0.957 → 数学 4/4 具体

## 知识整合 (train_integrated.py) — 共享 LIF 逻辑引擎

一个模型 (统一词表 + 共享嵌入 + 共享 LIF + 3 任务头):

| 任务 | 独立 | 整合 |
|:---|:---:|:---:|
| 受控语言 | 0.957 | **0.953** (无损) |
| 符号逻辑 | 0.860 | 0.783 (小降) |
| 数学 | 0.78 | 0.533 (降, 结构差异) |

**结论**: 单独知识可整合 — 共享 LIF = 通用逻辑引擎
- 语言/符号整合近无损 (结构相似: 单序列)
- 数学降 (多前提结构 → 需任务特定编码前端)
- 设计: 共享引擎 + 任务特定前端 (结构适配)

## 同侧协调分区 (train_dual_zone.py) — 语言保持✅ 数学待诊

一套池 + 区域 token 前缀路由 (语言区/数学区):

| 任务 | 独立 | 共享 | 分区 |
|:---|:---:|:---:|:---:|
| 语言 | 0.957 | 0.953 | **0.947** ✅ |
| 数学 | 0.78 | 0.53 | 0.482 ⚠️ |

**结论**: 区域路由有效 (语言不干扰, 池分区);
数学低 = 待区分 (任务本身难 0.78 vs 整合训练不足)
- 需诊断: 数学独立同结构训练 (区分干扰 vs 欠训练)
- 方向: 区域化 = 同侧分工 (布洛卡/角回), 机制在位

## 数学区数字理解诊断 (诚实) — 未完全理解数字

嵌入分析 (0-9):
- 相邻相似度 0.079 (略高) vs 远距 -0.096 — 部分结构
- 最近邻=相邻数字: 3/10 ❌
- PCA 数值线: 非单调 ❌ (0/1 一端, 8/9 一端, 中间起伏)

**结论**: 数学区 = 符号匹配 + 部分数值线索, 非真数值理解
- 等式判断可模式匹配 (不需数值语义) → 未学到数值顺序
- 改进: 大小比较监督 / 显式数值编码 / 计算任务

## 数感教学 (train_number_sense.py) — 数字理解成功! ✅

"从 0 教小孩" 课程: 数数 (顺序) → 数量 → 大小比较

| 验证 | 结果 |
|:---|:---:|
| 数数 acc | 1.000 |
| 大小比较 acc | 1.000 |
| **线性探针 (数值方向)** | **相关 1.000 (embed(a)·d = a 精确!)** |

**对比**: 等式训练 (无数值, 探针无) vs 数感教学 (数值 tag 1.000)
= 符号 → 数量 tag → 关系 的教学法让数学区真理解数字
  分布式数量表征 (多维度, 线性可读)

**教学原则**: 数字理解需要数量任务 (数数/比较),
非仅等式模式匹配 (之前诊断的根因)

## 算术 + 温故知新 (train_arithmetic.py)

| 模式 | 数数 | 比较 | 算术 |
|:---|:---:|:---:|:---:|
| 温故知新 (交错复习) | 1.000 | 1.000 | 0.364 |
| 只学新 | 1.000 | 0.989 | 0.436 |

**温故知新**: 旧知识保持 ✅ (任务简单饱和, 差异不显著)
**算术 0.36-0.44 ⚠️**: 符号匹配查表 (55 对), 非数量操作
→ 需数量操作教学: 3+2 = tag(3)+tag(2) = 5 单位 (数值轴加法)
  (与数字理解同教训: 从数量教, 非符号匹配)

## 位置系统数数 (train_counting.py) — 0-19 学会, 举一反三待实例

| 范围 | acc |
|:---|:---:|
| 训练 (0-19) | 1.000 ✅ |
| 举一反三 (20-49) | 0.000 ❌ |
| 举一反三 (50-98) | 0.000 ❌ |

**根因**: 0-19 只有一个进位实例 (9→10) → 模式记忆, 非规则
**教学教训**: 举一反三需要足够实例 (儿童数到 20/30/40 才归纳进位)
改进: 训练 0-49 (4 次进位) → 测 50-99; 或 0-99 → 100+

## 数数泛化诊断 (0-69 实例仍不泛化) — 查表 vs 规则

| 训练 | 训练内 | 未见 |
|:---|:---:|:---:|
| 0-29/49/69 | 1.000 | 0.000 全部 |

**根因**: 不是实例不足 — 监督查表不归纳"位值规则"
- 模型学 [十位,个位] 模式对查表, 未学"个位循环 + 进位"规则
- 位值 = 位分离规则 (个位 (x+1)%10 独立循环; 个位9→十位+1)

**深层教训** (数感/算术/数位 同一):
规则性任务需要规则结构/规则监督, 查表不泛化
- 数字: 数量 tag (探针 1.000)
- 算术: 数量操作 (非查表)
- 数位: 循环/进位规则 (位分离)

## 自我训练 (o1 式: 生成-验证-改进) — 机制✅ 逐步慢

每轮: 自主数数 → 可计算验证 → 修正监督 (教师=计算)
结果: 8 轮数到 8 (每轮 +1), 0-19 0.42, 20+ 0.000

**结论**: 自我训练机制可行 (自动验证 = 计算, 无需标注)
但查表学习 → 逐步慢 + 不归纳进位 (同前 3 次诊断)
- 需要: 批量教学 / 进位=LIF事件(规则结构) / 规则奖励(o1 RL式)

## 自主学习框架 (self_learning_framework.py) — o1 式, 计算验证 ✅

循环: explore(生成) → verify(可计算检查) → learn(修正) → curriculum(扩展) → log

**结果**: 12 轮自主数数 → 自己数出进位 (0...13, 10+ 突破!)
- 奖励源 = 计算验证 (n+1 可算) — 无人工标注 (vs o1 RLHF!)
- 课程: 能力驱动 (数到哪 → 目标+20)
- 轨迹: LearningLog (JSON, 可解释)

**优势**: 可计算域 (数字/逻辑/数学) = 自我奖励信号
= "自己训练自己" 的干净实现
