# 泛化领域验证: Gym 连续控制 (2026-08-15)

> 原则: 纯自研框架 (世界模型+生长+学习V+MPC), 不依赖外部 RL 框架 (PPO 等)。
> 迁移实验有其他策略参与 → 不作数。

## 环境
- CartPole-v1: 4维状态, 离散2动作, 密集奖励
- BipedalWalker-v3: 24维状态 (髋/膝关节), 连续4动作 (关节扭矩), 稀疏奖励

## CartPole ✅ 框架迁移成立

| 组件 | 结果 |
|:---|:---|
| 世界模型 (s,a)→(s',r) | 预测误差 0.0001 (完美学动力学) |
| MPC (手动规则评分) | 118 步 vs 随机 22 (5.4×) |
| **学习 V + MPC (纯框架)** | **103 步 vs 随机 22 (4.7×)** |

学习 V 教训: 从单一行为策略的 MC 回报学偏 (9步); 混合数据 (随机+启发式) 修复 (103步)。
→ 价值学习依赖行为策略覆盖 (方法论发现)。

## Walker ⚠️ 诚实边界

| 组件 | 结果 |
|:---|:---|
| 世界模型 | 预测误差 0.05 (只能学"跌倒动力学") |
| 采样 MPC (手动/学习V) | -105 (走不了几步) |

根因 (非缺 PPO):
1. 随机策略平均 10 步倒 → 状态覆盖极小, 数据无"走路"样本
2. V 从全负回报学, 无走路信息
3. 24维连续动作空间, 一步随机采样难找到好动作

## 用户洞察: 运动神经元协作 (待验证)

Walker 4 关节 (2髋+2膝) = 运动神经元集群协作。
验证逻辑: 策略隐层神经元 ↔ 关节状态 互信息分组 → 删组 → 该关节失灵。
当前阻塞: Walker 探索不足 (需自举或更简单环境)。

可选路径:
- A. 自举探索 (世界模型引导收集, 多轮) — 纯框架, 耗时
- B. Acrobot (2关节摆, 离散) — 协作明确, 较简单
- C. 自构 2-3 关节环境 (可控难度)

## 原则 (用户规定)

- 框架不依赖其他强化学习框架 (PPO/DQN/标准REINFORCE 不作数)
- 迁移实验有其他策略参与 → 不作数
- 决策来自框架自身: 世界模型 + 生长神经 + 学习价值

## 统一入口 (train_gdsp.py) — 迁移完整性 ✅

生长/淘汰/睡眠为**必需运行时组件**, 迁移 = 换环境+维度。

| 环境 | 结果 | 状态 |
|:---|:---|:---|
| CartPole | 世界模型 0.0046, 难样本生长 14, MPC 66-120 步 (随机 22) | ✅ 纯框架成立 |
| Walker | 生长自动接入 (10-14), 世界模型误差 0.16 | ⚠️ 探索边界 |

**难样本定向生长** (克隆预测误差最大样本激活神经元 → 专精难预测状态):
- CartPole 生效 (生长自动)
- Walker: 删生长神经元 → 临界 +6% vs 平衡 +16% — 无临界专精
  根因: 随机数据覆盖不足, 所有状态误差相当 (0.16 vs 0.16), 难样本无区分度
  → 生长信号需要有意义的经验 (探索增强/自举), 非机制失效

**诚实边界**: Walker (24维连续+稀疏奖励) 的走路 + 关节协作验证,
需要探索增强 (自举) 或更简单多关节环境 — 留作后续。

## Successor Measure 版 (walker_sr.py) — 自监督安全访问价值 ✅

SR 思想: V_surv = E[Σ γ^t · 1(safe)] (未来安全状态访问, TD 自监督, **无奖励依赖**)
→ 修复 MC-V 的"奖励分布漂移崩溃"根因 (轮 3-7 全崩)

| 版本 | 峰值存活 | 崩溃模式 |
|:---|:---|:---|
| MC-V (奖励回报) | 7500 (含bug) | 轮 3-7 全 0 崩溃 |
| **SR (安全访问)** | **925-1148 步** | 波动 (70-1100), 无全崩 |

**结论**:
1. SR 思想有效: "生存 = 最大化未来安全状态访问" 不需奖励函数,
   TD 自监督从自举状态转移学 — 比奖励回报更稳定 (不因奖励漂移崩)
2. 峰值 ~1000 步 = agent 学会基本平衡 (随机 ~10 步)
3. 完整走路 + 关节协作仍受数据/稳定性限制 (世界模型增量漂移, V 无连续性)

**启发**: 生存目标可以不用奖励函数定义 — 用"未来状态访问" (successor
measure) 表达。这与我们的主张一致: 结构 (世界模型/生长) 学环境,
价值是薄层 (安全访问权重)。

## 抗扰动评估 (walker_robustness.py) — SR 结构性稳健

扰动矩阵: 动作噪声(0.1/0.2) / 观测噪声(0.05/0.1) / 初始倾斜(0.2/0.4) / 综合

| 策略 | 扰动下存活波动 |
|:---|:---|
| **SR (安全访问)** | **<±8%** (77→72~83 步, 稳健) |
| MC-V (奖励回报) | +29~57% (噪声打破死循环, 非优点) |

**结论**:
1. SR 抗扰动结构性优于 MC-V: 安全访问价值平滑, 不依赖奖励尖峰
2. MC-V 噪声增益 = 基线陷入 MPC 死循环, 噪声是解药 (基线弱)
3. 诚实: 弱基线 (未完全收敛), 需完整训练定论

**SR 额外优势**: 抗扰动 + 无奖励依赖 + 换目标灵活 (Q=M·r) —
successor measure 是生存价值函数的更稳健选择。

## 生长功能性 (Walker) — 诚实负结果

| 条件 | 存活 | 变化 |
|:---|:---:|:---:|
| 完整模型 (36 生长) | 80 步 | — |
| 删 36 个生长神经元 | 278 步 | +247% (反而更好) |
| 删 36 个随机初始 | 100 步 | +25% |

**结论: 生长神经元在 Walker 无功能性 (有害)**。

**根因 (方法论发现)**:
- 生长的功能前提 = **可靠的世界模型基础**
- 符号: 模型误差 0.02 → 难样本=真难 → 克隆有效 (1.47×)
- Walker: 模型误差 0.16 → 难样本=噪声 → 克隆坏神经元 → 干扰决策
- 误差驱动生长在弱模型下学噪声 (有害), 强模型下学结构 (有效)

**启示**: 生长机制与符号实验一致 (机制无误), 失败是基础质量 —
验证生长功能前, 必须先有可靠世界模型 (探索/数据覆盖)。

## 整套迁移 (walker_full.py) — 做梦淘汰坏神经元 ✅ 部分验证

昼夜循环: 白天(世界模型训练+难样本生长) → 夜晚(做梦: 生存优先片段回放 + 淘汰)

| 版本 | 峰值存活 | 删生长神经元 | 生长存活 |
|:---|:---:|:---:|:---:|
| SR 无做梦 | 1148 | +247% (有害) | 36 (全留) |
| **SR+做梦+淘汰** | **1417** | **+8% (中性)** | **14 (淘汰22)** |

**做梦的机制验证**:
1. 生存优先回放 (活得久的轨迹片段×随机方向) → 世界模型强化好结构
2. 坏神经元 (对好轨迹不激活) → 激活率低 → 淘汰回收 (36→14)
3. 删生长从 +247% (有害) → +8% (中性) — "学噪声"被治愈

**部分验证**: 做梦把坏神经元清了 (方向正确), 但存活者仍中性
非功能性 — 需要更强世界模型 (数据/探索) 才能让存活者真正专精。
"做梦调神经元向好" = 淘汰坏 + 巩固好, 机制在控制域成立。

## 基础强化 (Δ预测 + buffer + ε探索) — 误差减半 ✅

| 版本 | 世界模型误差 | 峰值存活 |
|:---|:---:|:---:|
| 绝对预测 (GDSPModel) | 0.16 | 1417 |
| **Δ残差预测 (DeltaWM)** | **0.082** | 1125 |

**基础强化有效**: Δ预测 (s'=s+Δ) 对连续高速动力学误差减半 (0.16→0.082)。
但存活未同步提升: 0.08 误差在 24 维连续控制仍高 (MPC 累积), 需更多数据/轮数。

**Walker 完整走路 = 长线任务**: 需 30+ 轮/更大 buffer/更强探索。
今日验证的框架组件 (全部控制域成立):
1. 统一入口 (迁移不漏组件) ✅
2. SR 价值 (无奖励, 抗扰动 <±8%) ✅
3. 做梦净化坏神经元 (删生长 +247%→+8%) ✅
4. Δ预测基础强化 (误差减半) ✅


## 定位修正: Δ预测是工程技巧, 不是机制

- Δ残差预测 (s'=s+Δ): 标准回归技巧 (时序差分/ResNet/物理预测均有)
- 作用: 强化基础 (世界模型质量), 让生长/做梦有可靠信号源
- 框架机制 (研究贡献): 生长/淘汰/做梦/SR价值/连接自适应
- 论文定位: 机制是贡献, 技巧 (Δ/归一化/buffer) 是复现细节

## 并行环境数据量实证 (walker_parallel.py, 8 envs × 20 轮 = 1M 步)

存活: 78,226,92,465,94,89,1047,198,167,135,803,77,85,407,130,554,430,414,106,572
- 中位 ~180 步 (18× 随机 10), **峰值 1047 (接近 1600 平衡上限)**
- 无单调上升, 高波动 (77-1047)

**数据量假设部分验证**:
- 90K 步: 存活 70-300
- 1M 步: 中位 180, 峰值 1047 (偶尔完全平衡)
- 稳定走路需 5M+ 或更好方法 (非单纯堆数据)

**瓶颈**: 波动=决策不可靠 (非数据量单一因素)
- MPC 单步 K=200 噪声 / V_surv 精度 / 世界模型 0.08 累积
- 改进: 多步 rollout / 课程(先平衡后走路) / 更大容量

## 时序升级: 记忆池 (LIF) — 神经元状态转换能力 ✅ (cycle_task.py)

任务: 状态机 S0↔S1 周期 T=8 切换, 相位不可见 (只能靠记忆推断)。

| 池 | 切换预测误差 | 稳定误差 |
|:---|:---:|:---:|
| 记忆池 (LIF+τ 分工) | **0.016** | 0.0001 |
| 静态池 (无记忆) | 0.766 | 0.0073 |

**记忆池 48× 改善 — 神经元短期记忆 (膜电位+τ) 确证发展出状态转换能力**:
- τ 多样化 → 快神经元 (瞬态) / 慢神经元 (节奏相位)
- 电位残留编码"在状态待了多久" → 预测切换时刻

**但难样本定向生长破坏该能力** (0.016→1.12), 两个根因:
1. 生长信号选错: 难样本=切换时刻 → 克隆"切换响应器" (瞬态)
   非"相位检测器" (τ大, 持续积累) — 单步误差 ≠ 时序结构缺失
2. 容量过剩: 512 池学周期绰绰有余, 生长=冗余干扰
   生长的价值在容量不足时 (扩容), 非容量够时

**框架时序能力现状**: 世界模型单步 → 记忆池提供短期记忆 (时间维)
生长信号需"时序难"定义 (多步结构缺失), 非单步误差尖峰。

## 生长→试探→连接 (baby 机制) — 修复生长功能性 ✅

用户洞察: 克隆不可能继承全部信息 (膜电位/连接建立), 需要发育过程。
实现: 弱初始化(0.5·W+0.3·randn) + 电位零 + τ发展 → 试探期(低权重参与+
强制探测 p=0.3) → 试用期评估(激活达标→巩固 / 不达标→剪枝)。

| 生长方式 | 切换预测 | 删生长神经元 |
|:---|:---:|:---:|
| 无生长 | 0.016 | — |
| 直接克隆 | 1.12 (破坏) | -89% (删了反而好) |
| **baby 机制** | 0.117 | **+378% (功能性!)** |

**验证**: 生长→试探→连接 让新生神经元发展自己的功能 (删了伤切换预测),
直接克隆产生冗余响应器 (破坏读出)。"连接能力"不能继承, 必须试探获得。

**框架时序能力完整闭环**:
1. 记忆池 (LIF+τ): 神经元状态转换能力 ✅ (切换 0.016 vs 静态 0.77, 48×)
2. 生长修正: 生长→试探→连接, 生长功能性 ✅ (+378%)
3. 生长的功能前提增加维度: 空间(结构难/噪声难) + 容量(不足时) + 连接(试探获得)

## Walker 记忆版 (walker_mem.py) — 新机制迁移, 初步无显著提升

6 轮 (180K 步): 68, 91, 933, 144, 184, 114 (峰值 933, 中位 ~144)
vs 无记忆 ΔWM 20 轮: 峰值 1047, 中位 ~180

**诚实结论**: 记忆机制 (LIF+baby) 在 Walker 初步无显著提升。
- 符号周期任务: 记忆=关键 (切换 0.016 vs 0.77, 生长 +378%)
- Walker: 时序不是首要瓶颈 (基础精度 0.08 才是)
  L=12 历史 < 步态周期; 轮数不足 (无记忆版 20 轮才见峰值)
- **记忆 = 时序任务的必要条件, 不是 Walker 的充分条件**
  Walker 瓶颈在基础 (世界模型精度), 时序是叠加层

**统一框架判断**:
- 生长功能前提: 空间(结构/噪声) + 容量 + 连接(试探) + 时间(记忆, 时序任务)
- 记忆机制价值与任务时序需求成正比 (符号: 高, Walker: 低)

## 做梦生长 + 方向塑造 (dream_grow_task.py) — 生长功能性 ✅

用户设计: 训练中制造生长方向 (储备 shadow 预测暗中学习), 做梦期生长
(激活对难样本预测最好的储备神经元) — 生长即用, 无长试探期。

| 模式 | 切换预测 | 删生长神经元 |
|:---|:---:|:---:|
| dream (做梦生长) | 0.54 | **+140% (功能)** |
| train (克隆, 有 shadow) | 0.0000 | +22亿% (功能但过度依赖) |

**验证**:
1. 做梦生长 (激活有方向储备) → 功能性 ✅ (+140%)
2. shadow 方向塑造本身可能提升整体 (有 shadow 后 train 克隆 0.0000
   vs 无 shadow 旧版 1.12) — 储备暗中学习 = 附加预测通道

**诚实**: 变量混杂 (shadow+模式同时变), 需干净消融;
dream 整体切换 0.54 非最优 (选择策略待优化)。

**生长机制谱系** (统一):
  直接克隆 → 破坏 (继承冗余, 无方向)
  baby 试探 → 功能 (试探期发展, 慢)
  做梦生长 → 功能 (白天塑造方向, 即用) ← 用户设计

## 储备生命周期 (凋亡/再出生) — 完整闭环 ✅

设计: 储备神经元塑造期 → 成熟评估 (shadow 选中率) → 
  高贡献保留 / 低贡献凋亡 → 再出生 (随机重初始化重新塑造)。

| 模式 | 切换预测 | 删生长 | 储备流动 |
|:---|:---:|:---:|:---|
| dream (无生命周期) | 0.54 | +140% | — |
| **dream + 生命周期** | **0.26** | **+480%** | 凋亡 1828 / 保留 1172 |
| train + 生命周期 | 0.0054 | +28% | 凋亡 1864 / 保留 1152 |

**验证**:
1. 储备生命周期工作: 大多数储备凋亡再出生 (生物: 成年神经发生
   大多数新生神经元凋亡), 一部分保留 (有方向)
2. 生命周期改善 dream 生长: 切换 0.54→0.26 (2×), 
   生长功能 +140%→+480% (3.4×) — 淘汰坏储备 → shadow 通道稳定
3. 完整机制: 训练塑造方向 (shadow) + 做梦生长 (激活储备) 
   + 生命周期 (凋亡/保留/再出生) → 生长功能性 +480%

**神经发生完整闭环**: 出生(随机) → 塑造(shadow训练) → 成熟评估 →
  激活整合(dream_grow) 或 保留(有方向后备) 或 凋亡(再出生)

## 腿间扰动验证 (消融) — 一条腿是对另一条腿的扰动 ✅

预测腿1 (躯干速度+关节+接触), 输入消融:

| 输入 | 腿1误差 | 接触切换 |
|:---|:---:|:---:|
| 腿1 自身 | 0.0485 | 0.0726 |
| + 腿2 状态/动作 | 0.0414 (↓15%) | 0.0584 (↓20%) |
| 全量 | 0.0411 (↓15%) | 0.0577 |

**验证**:
1. 腿1 误差 15% 归因于腿2 (对侧腿 = 结构性扰动源, 非噪声)
2. 接触切换时刻更依赖对侧 (20%) — 触地预测需知道对侧腿动作
3. 自身动力学仍主导 (85%)

**设计**: 按腿拆分世界模型 (每腿专家 + 对侧扰动条件化)
**接触感官**: 无新增 (BipedalWalker 观测已含接触标志 dim 8/10)
**容量**: 0.082 → 0.026 (3×) — 真增益在容量, 非感官

**Walker 瓶颈精确化**: 接触动力学 (切换时刻难学) + 腿间耦合 (15%) 
+ 容量/数据 — 拆分学习形式 = 按腿专家 + 扰动输入

## ⚠️ 重要修正: Walker 评估接口 bug — 实际接近解决!

之前所有 Walker 评估用 train_gdsp.mpc_act (GDSPModel 接口, 解包 5 值),
但 walker_full 的 DeltaWM 返回 3 值 — 接口不匹配 → 假 80 步。

正确接口 (DeltaWM.rollout) 评估 walker_full.pt:
  平均 219 步, 中位 208, **29/60 (48%) 达 300 上限**
  → Walker 已接近解决 (ΔWM + SR价值 + 并行自举)!

**之前"Walker 波动/失败/需要长线"结论需要更正**:
  - 波动假象部分来自错误评估 (80 步)
  - 真实: 219 步, 48% 完整 episode

**振荡器 (节奏感) 对照** (正确接口):
  MPC 自由探索: 219 步 (48% 上限)
  状态耦合振荡器锁定: 78 步 (0%) — 限制探索, 节律模式不匹配
  相位多样化采样: 274 步 (1.06×) — 轻微增益
  → 节律先验在 MPC 下收益有限, 世界模型+价值已隐式学步态

**心跳类比修正**: 状态耦合节律概念正确 (生物), 但实现应为
"调制探索分布"而非"锁定动作"; 且增益小 (模型已学会)。

## Walker 真实状态 (撤销 300 上限, 环境默认 1600)

正确接口评估 walker_full.pt (撤销评估截断):
- 平均 771 步 (随机 ~10 = 77×), 中位 207
- **17/40 (42.5%) 完整走完 1600 步** — 部分学会走路!
- 平均奖励 -114 (混合: 42.5% 完整 vs 57.5% 早期倒)

**修正**: 300 是评估截断非环境上限 (1600)。
之前 219 步 (300 上限) → 真实平均 771 步。

**Walker 状态**: 部分学会 (42.5% 完整走路), 不稳定 (中位 207)。
剩余问题: 随机地形障碍 (台阶/坑) + MPC 单步决策 + 误差累积。
不需要"更多数据" — walker_full 仅 80K 步训练。

## Walker 解决: 瓶颈 = 决策视野 (MPC 单步) — 多步 MPC ✅

多步 MPC (预测 D 步后 V 评分):

| D | 平均 | 中位 | 完整走完 |
|:---|:---:|:---:|:---:|
| 1 (单步) | 742 | 250 | 4/10 |
| 2 | 1024 | 1600 | 6/10 |
| 3 | **1374** | **1600** | **7/10 (70%)** |

**瓶颈精确链** (回答"没视觉?"):
- lidar 有 (10 维) ✅, V 对 lidar 敏感 (近障碍 V 0.31 vs 远 0.48) ✅
- 但 MPC 单步视野短 → 看到障碍时已晚
- 多步 MPC: 预测 3 步 → 提前看到障碍 → 调整步伐

**Walker 完整解决**: ΔWM + SR价值 + 并行自举 (80K 步) + 多步 MPC
= 70% 完整走完 1600 步, 平均 1374 (随机 10 步 = 137×)

**不需要**: 更多数据 / 视觉 (已有) / 节律先验
**需要**: 决策视野 (多步规划) — 世界模型多步 rollout 误差可控

## ⚠️ 诚实修正: Walker "存活" = 平衡站立, 非走路

- SR 安全访问价值最优策略 = **静止站立** (不动最安全): 存活 1600 步 (3/3 完整) 但位移 ≈0
- 加前进项 (γ·vx) / 相位多样化采样 → 位移仍 ≈0 (γ 大则崩)
- **走路 = 协调时序动作序列, 超出随机采样 MPC 能力** (4 维连续动作 × 周期协调)
- 框架边界: Walker 学会平衡 (不跌倒), 未学会移动 (觅食移动在迷宫因探索奖励成立)
- "存活步数" = 平衡时间, 不是移动距离 — 数值正确但语义是"不跌倒"

## 抽象层 (abstract_layer.py) — 架构✅ 决策层缺陷(系统性)

架构: 场景层 → 语义特征 z → 共享神经元池 → 每场景评分
- 神经元抽离场景 ✅ (工作在任何 z 空间)
- 跨场景复用 ✅ (池权重迁移)
- 每场景设定值 ✅ (FoodGame 得分=食物; EnergyGame 得分=能量)

结果: 场景A 学习后 2.8 (随机 73.5), 场景B 迁移 0.0 (随机 33.3)

**系统性缺陷确证** (第 6 次同一模式):
"世界模型 + TD价值 + 采样MPC" 决策范式在行动密集任务失败:
  TD 学"静止安全" → MPC 选静止 → 比随机差
  (能量觅食 / Walker 走路 / 抽象场景 — 同根因)

**框架级结论**: 
- 抽象层方向正确 (场景无关表示 + 复用)
- 决策层需要"策略层" (直接学动作分布, 打破静止陷阱)
- 与 Loihi 2 结合: 策略 = 神经元层直接输出动作概率 (在线学习)

## 目标驱动专精 (goal_growth.py) — 生长方向由目标决定 ✅

三种生长信号 → 删生长神经元 → 得分变化 (食物收集任务):

| 生长信号 | 完整得分 | 删后 | 变化 | 判定 |
|:---|:---:|:---:|:---:|:---|
| 随机 | 6.2 | 6.7 | -8% | 中性 (无方向) |
| 误差驱动 | 3.5 | 3.2 | +10% | 有害 (学噪声) |
| **目标驱动** | 4.7 | 5.3 | **-14%** | **功能性 (专精目标)** |

**回答核心问题**:
1. 神经元生长有专精方向 → 有, 由生长信号决定
2. 自发涌现专精由目标驱动 → ✅ 目标驱动生长 → 功能性专精
   (删了 → 目标达成崩); 误差驱动 → 学噪声 (删了反而好)

**统一谱系** (解释所有先前"失败"):
- 误差驱动生长学"预测难处" — 在目标导向任务 = 噪声 (Walker/能量/抽象层全中)
- 目标驱动生长学"目标关键状态" — 功能性专精
- Walker 需要驱动力 (目标) 的原因: 无目标 → 生长学噪声 → 无专精

## Walker 能量+驱动规则环境 (walker_energy_env.py) — 设计✅ 学习框架缺陷确证(第7次)

用户设计修正 (全部实现):
① 能量 = 输出层隐形约束: E -= Σ|τ·Δangle| + 基础代谢 (神经元活动免费)
   阶段目标 (前进40) → +40 能量 (不区分目标)
② 驱动规则 (环境层, 与 MPC 无关): vx < 0.4 连续 80 步 → 死
   验证: 零动作 81 步死 ✅; 大输出 42 步快耗 ✅

学习结果: MPC 存活 81 (死亡线), 前进 2 — 仍不移动
根因: 随机探索"移动"= 低效 (高输出快耗) → 价值学"移动=快死" → 少动
= **"世界模型+TD价值+采样MPC" 无法学习行动策略 (第 7 次确证)**

**框架级结论**: 评估(价值)+采样(MPC) ≠ 行动策略学习
→ 需要"策略层" (直接学习动作分布) — 下一步核心组件

## EGGROLL 风格 ES 策略 (es_strategy.py) — 策略层解决 ✅ + 生长冲突确证 ⚠️

| 方法 | 得分/ep (食物迷宫) |
|:---|:---:|
| 随机 | 73.5 |
| 价值+MPC (7次失败) | 2.8 |
| **纯 ES (低秩进化)** | **80.0 ✅** |
| ES+生长 | 6.3 (崩) |
| 删生长 | 13.3 (-111%, 删了更好) |

**① 策略层缺口解决**: ES 直接优化得分 (backprop-free, 低秩扰动)
→ 打破"价值+MPC"静止陷阱 (80 vs 2.8, 28×)

**② 生长-ES 冲突确证**: 
- ES = 参数空间连续搜索 (爬山)
- 生长 = 结构突变 (克隆+权重跳变) → 改变地形 → 优化路径失效
- 删生长 → 回到优化状态 → 得分回升
- 时间尺度不匹配: 连续参数优化 vs 离散结构跳变

**调和方向**: 生长后长适应期 / 温和生长(小量) / 分段(生长期→优化期)
**统一**: 结构可塑性 (生长) 与参数优化 (ES) 需协调调度 — 生物对应:
  发育期(结构变化大) → 成年期(结构稳定, 参数微调)
